function refreshContact(innerHTML) {
  const contactShow = document.getElementById('contact-details');
  contactShow.innerHTML = innerHTML;
}

refreshContact('<%= j render "contacts/show_partial", contact: @contact %>');
